/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf64-x86-64)
INPUT ( -lgfortran_nonshared /usr/lib64/libgfortran.so.3 AS_NEEDED ( -ldl ) )
